# Calculate the bmi using weight and height.

height = 1.65 
weight = 84
bmi = (weight/(height**2))
print(bmi)
print(int(bmi))
print(round(bmi))
print(round(bmi,2))

#to print the output of the bmi

if bmi < 18.85:
    print("underweight")
elif bmi >=18.8 and bmi <25:
    print("normal weight")
else:
    print("overweight")

